#!/bin/bash

set -Eeuo pipefail

if [ ! -f var/fantasyApp.sqlite3 ]
then
    ./bin/db create
fi

ssh -i fantasy_report.pem ubuntu@ec2-3-15-44-26.us-east-2.compute.amazonaws.com
cd fantasy_report
source env/bin/activate
git pull
pkill -f gunicorn
gunicorn -b localhost:8000 -w 2 -D fantasyApp:app